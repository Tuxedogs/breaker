diff --git a/src/components/industry/mining/LocationDetail.tsx b/src/components/industry/mining/LocationDetail.tsx
--- a/src/components/industry/mining/LocationDetail.tsx
+++ b/src/components/industry/mining/LocationDetail.tsx
@@ -18,10 +18,11 @@ import {
   buildResourceRows,
   formatPercent,
   methodBiasToneClass,
+  miningMethodBadge,
   qualityChanceHeader,
   resourceRowMaterialKey,
   scoreToneClass,
-  systemBadgeClass,
+  systemBadgeClass,
 } from "./miningFormatters";
@@ -174,6 +175,20 @@ function MiningMobileStat({ label, value, toneClass }: { label: string; value: s
   );
 }
 
+function MobileMaterialStatusPill({
+  label,
+  status,
+}: {
+  label: string;
+  status: DemandRow["status"] | ResourceRow["status"];
+}) {
+  return (
+    <span className={`mdet-mobile-status-pill mdet-mobile-status-pill--${status}`}>
+      {label}
+    </span>
+  );
+}
+
 function MiningMobileMaterialCard({
   row,
   mode,
@@ -183,24 +198,28 @@ function MiningMobileMaterialCard({
   mode: "demand" | "resource";
   qualityHeader: string;
 }) {
-  const methodLabel = mode === "demand"
+  const methodValue = mode === "demand"
     ? (row as DemandRow).coverage === "Missing"
       ? "Missing"
       : row.miningType
     : row.miningType || "Unknown";
+  const methodBadge = miningMethodBadge(methodValue);
+  const methodLabel = methodBadge?.label ?? (methodValue || "Unknown");
   const primaryQualityLabel = mode === "demand"
     ? (row as DemandRow).targetQualityChanceLabel
     : (row as ResourceRow).qualityLabel;
 
   return (
     <article className={`mdet-mobile-material-card mining-resource-row--${row.status}`}>
-      <div className="mdet-mobile-material-head">
-        <div className="mdet-mobile-material-title">
-          <MaterialNameCell name={row.name} miningMethod={row.miningType} iconSize={18} />
+      <div className="mdet-mobile-material-main">
+        <div className="mdet-mobile-material-head">
+          <div className="mdet-mobile-material-title">
+            <MaterialNameCell name={row.name} miningMethod={row.miningType} iconSize={18} />
+          </div>
+        </div>
+        <div className="mdet-mobile-material-meta">
+          <span className={`mdet-mobile-source-chip${methodBadge ? ` mloc-badge ${methodBadge.className}` : ""}`}>{methodLabel}</span>
+          <MobileMaterialStatusPill label={row.densityLabel} status={row.status} />
         </div>
-        <span className="mdet-mobile-material-method">{methodLabel}</span>
-      </div>
-      <div className="mdet-mobile-material-encounter">
-        <MiningSourceBadge status={row.status} densityLabel={row.densityLabel} sourceWeight={row.sourceWeight} title={"sourceTitle" in row ? row.sourceTitle : undefined} />
       </div>
       <div className="mdet-mobile-stat-grid">
         <MiningMobileStat label={qualityHeader} value={primaryQualityLabel} />
diff --git a/src/components/industry/mining/mining.css b/src/components/industry/mining/mining.css
--- a/src/components/industry/mining/mining.css
+++ b/src/components/industry/mining/mining.css
@@ -1677,19 +1677,88 @@
 
 .mdet-mobile-material-card {
   display: flex;
-  flex-direction: column;
-  gap: 0.55rem;
-  padding: 0.78rem 0.82rem;
-  border: 1px solid rgba(255, 255, 255, 0.07);
-  border-radius: 8px;
-  background: rgba(10, 14, 22, 0.72);
+  align-items: flex-start;
+  gap: 0.7rem;
+  padding: 0.72rem 0.82rem;
+  border: 1px solid rgba(255, 255, 255, 0.065);
+  border-radius: 7px;
+  background: rgba(11, 15, 22, 0.82);
 }
 
+.mdet-mobile-material-main {
+  display: flex;
+  flex: 1 1 auto;
+  min-width: 0;
+  flex-direction: column;
+  gap: 0.32rem;
+}
+
 .mdet-mobile-material-head {
   display: flex;
-  align-items: flex-start;
-  justify-content: space-between;
-  gap: 0.6rem;
+  align-items: center;
+  gap: 0.55rem;
 }
 
 .mdet-mobile-material-title {
   min-width: 0;
   flex: 1 1 auto;
 }
 
 .mdet-mobile-material-title .mining-material-name-cell {
-  align-items: flex-start;
+  align-items: center;
+  gap: 0.48rem;
 }
 
 .mdet-mobile-material-title .mining-material-name-text {
   white-space: normal;
   overflow: visible;
   text-overflow: unset;
-  line-height: 1.05;
+  line-height: 1.04;
+  font-size: 1rem;
 }
 
-.mdet-mobile-material-method {
-  flex: 0 0 auto;
-  padding: 0.18rem 0.45rem;
+.mdet-mobile-material-meta {
+  display: flex;
+  align-items: center;
+  flex-wrap: wrap;
+  gap: 0.34rem;
+}
+
+.mdet-mobile-source-chip {
+  display: inline-flex;
+  align-items: center;
+  min-height: 1.2rem;
+  padding: 0.08rem 0.38rem;
   border-radius: 999px;
-  border: 1px solid rgba(255, 153, 0, 0.14);
-  background: rgba(255, 153, 0, 0.06);
-  color: rgba(255, 190, 80, 0.82);
   font-family: var(--font-primary);
-  font-size: 0.62rem;
+  font-size: 0.58rem;
   font-weight: 700;
-  letter-spacing: 0.08em;
+  letter-spacing: 0.09em;
   text-transform: uppercase;
+  line-height: 1;
   white-space: nowrap;
+  color: rgba(170, 187, 212, 0.72);
+  background: rgba(255, 255, 255, 0.035);
+  border: 1px solid rgba(255, 255, 255, 0.08);
 }
 
-.mdet-mobile-material-encounter {
-  display: flex;
+.mdet-mobile-source-chip.mloc-badge {
+  font-size: 0.58rem;
+  letter-spacing: 0.09em;
+  padding: 0.08rem 0.38rem;
+}
+
+.mdet-mobile-status-pill {
+  display: inline-flex;
   align-items: center;
+  min-height: 1.2rem;
+  padding: 0.08rem 0.36rem;
+  border-radius: 999px;
+  font-family: var(--font-primary);
+  font-size: 0.58rem;
+  font-weight: 700;
+  letter-spacing: 0.09em;
+  text-transform: uppercase;
+  line-height: 1;
+  white-space: nowrap;
+  border: 1px solid rgba(255, 255, 255, 0.08);
+  background: rgba(255, 255, 255, 0.035);
+  color: rgba(203, 213, 225, 0.72);
+}
+
+.mdet-mobile-status-pill--strong {
+  color: rgba(52, 211, 153, 0.88);
+  background: rgba(52, 211, 153, 0.08);
+  border-color: rgba(52, 211, 153, 0.18);
+}
+
+.mdet-mobile-status-pill--moderate {
+  color: rgba(255, 190, 80, 0.88);
+  background: rgba(255, 153, 0, 0.08);
+  border-color: rgba(255, 153, 0, 0.18);
+}
+
+.mdet-mobile-status-pill--low {
+  color: rgba(251, 146, 60, 0.84);
+  background: rgba(251, 146, 60, 0.08);
+  border-color: rgba(251, 146, 60, 0.16);
+}
+
+.mdet-mobile-status-pill--missing {
+  color: rgba(248, 113, 113, 0.78);
+  background: rgba(248, 113, 113, 0.07);
+  border-color: rgba(248, 113, 113, 0.16);
+}
+
+.mdet-mobile-status-pill--none {
+  color: rgba(148, 163, 184, 0.62);
+  background: rgba(255, 255, 255, 0.025);
+  border-color: rgba(255, 255, 255, 0.07);
 }
 
 .mdet-mobile-stat-grid {
   display: grid;
-  grid-template-columns: repeat(2, minmax(0, 1fr));
-  gap: 0.55rem;
+  grid-template-columns: repeat(3, minmax(0, 1fr));
+  gap: 0;
+  width: 100%;
+  border-radius: 5px;
+  background: rgba(255, 255, 255, 0.025);
 }
 
 .mdet-mobile-stat {
   display: flex;
   flex-direction: column;
-  gap: 0.16rem;
+  gap: 0.12rem;
   min-width: 0;
-  padding-top: 0.42rem;
-  border-top: 1px solid rgba(255, 255, 255, 0.06);
+  padding: 0.36rem 0.45rem 0.34rem;
+  border-left: 1px solid rgba(255, 255, 255, 0.06);
+}
+
+.mdet-mobile-stat:first-child {
+  border-left: 0;
 }
 
 .mdet-mobile-stat-label {
   font-family: var(--font-primary);
-  font-size: 0.6rem;
+  font-size: 0.54rem;
   font-weight: 700;
-  letter-spacing: 0.08em;
+  letter-spacing: 0.07em;
   text-transform: uppercase;
-  color: rgba(160, 180, 220, 0.52);
+  color: rgba(160, 180, 220, 0.5);
 }
 
 .mdet-mobile-stat strong {
   color: rgba(226, 232, 240, 0.9);
   font-family: var(--font-primary);
-  font-size: 1rem;
-  line-height: 1.05;
-  word-break: break-word;
+  font-size: 0.94rem;
+  line-height: 1;
+  word-break: keep-all;
+  white-space: nowrap;
 }
@@ -2709,11 +2778,47 @@
   .mdet-mobile-missing-block {
     display: flex;
     flex-direction: column;
-    gap: 0.6rem;
+    gap: 0.45rem;
   }
 
-  .mdet-mobile-stat-grid {
-    grid-template-columns: 1fr;
+  .mdet-header {
+    gap: 0.5rem;
+    padding-bottom: 0.34rem;
+  }
+
+  .mdet-header-left {
+    gap: 0.16rem;
+  }
+
+  .mdet-label {
+    font-size: 0.64rem;
+  }
+
+  .mdet-name {
+    font-size: 1.22rem;
+    line-height: 1.04;
+  }
+
+  .mdet-meta {
+    gap: 0.32rem;
+  }
+
+  .location-stat-chip-grid {
+    gap: 0.32rem;
+  }
+
+  .location-stat-chip {
+    gap: 0.1rem;
+    padding: 0.34rem 0.5rem;
+    border-radius: 0.48rem;
+  }
+
+  .location-stat-label {
+    font-size: 0.5rem;
+  }
+
+  .location-stat-value {
+    font-size: 0.88rem;
   }
@@ -2736,6 +2841,33 @@
     padding-inline: 0.45rem;
   }
+
+  .mdet-mobile-material-card {
+    padding: 0.66rem 0.72rem;
+    gap: 0.58rem;
+  }
+
+  .mdet-mobile-material-title .mining-material-name-text {
+    font-size: 0.94rem;
+  }
+
+  .mdet-mobile-source-chip,
+  .mdet-mobile-source-chip.mloc-badge,
+  .mdet-mobile-status-pill {
+    font-size: 0.54rem;
+  }
+
+  .mdet-mobile-stat {
+    padding-inline: 0.36rem;
+  }
+
+  .mdet-mobile-stat-label {
+    font-size: 0.5rem;
+  }
+
+  .mdet-mobile-stat strong {
+    font-size: 0.82rem;
+  }
