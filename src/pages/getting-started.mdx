# Getting Started

Install dependencies and run development:

```bash
npm install
npm run dev
```

Build for production:

```bash
npm run build
```

## Notes

- Vite handles local dev and build output.
- Tailwind powers utility-first styling.
- MDX pages are loaded directly as route content.
